<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comments</title>
    <link rel="stylesheet" type="text/css" href="./public/index.css" >
  </head>
  <body class="">
    {{! this will prob be written differently, only rendering one post that matches the postID }}
    {{#post}}
        {{body}}
        {{Author}}
        <form action="/club/like/{{id}}" method="post">
              <input type="hidden" name="postid" value="{{id}}">
              <button type="submit" formmethod="post" id="like-btn">Like</button>
        </form>
    {{/post}}
    {{#comments}}
        {{body}}
        {{author}}
         <form action="/club/like/{{id}}" method="post">
              <input type="hidden" name="postid" value="{{id}}">
              <button type="submit" formmethod="post" id="like-btn">Like</button>
        </form>
    {{/comments}}
    <form action="/submitcomment/{{id}}" method="post">
        <textarea rows="4" cols="50" name="commentBody">
        </textarea>
        <button type="submit" formmethod="post">Comment</button>
    </form>
  </body>
</html>